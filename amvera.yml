meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  npm:
    install:
      - install

artifacts:
  - from: /
    to: /

run:
  command: node server.js
  persistenceMount: /data
  containerPort: 3000