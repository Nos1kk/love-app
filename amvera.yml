meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  npm:
    - install

run:
  scriptName: server.js
  command: node server.js
  persistenceMount: /data
  containerPort: 3000
